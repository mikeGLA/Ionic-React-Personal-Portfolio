import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic-React-Personal-Portfolio',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
